{
  "name": "mox-lang",
  "version": "0.1.0",
  "scripts": {
    "start": "pnpm --filter mox start",
    "build": "pnpm run -r build",
    "generate": "pnpm --filter generate-ast generate ../mox/src"
  }
}
